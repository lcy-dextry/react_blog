import styled from "styled-components";

export const MessageWrapper = styled.div`
    .diary-list{
        width: 920px;
        padding: 40px;
        box-sizing: content-box;
        margin-bottom: 0;
    }
`